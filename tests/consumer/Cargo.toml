[package]
name = "consumer-smoke"
description = "Guards that cpsat works from outside its own workspace"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
cpsat = { path = "../../crates/cpsat" }
